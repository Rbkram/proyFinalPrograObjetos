Debe sustituir el nombre de usuario por el que usted utiliza o como segunda opción cambiar el puerto donde tenga el acceso a sqlServer.

Utilizar autenticación de windows para el server management.

Establecer la conexión, guardar el documento txt y ejecutar la aplicación.
Verifique que su conexión a internet tengo acceso a los puertos TCP y que las reglas del firewall se encuentren establecidas para el puerto por defecto de SQLserver